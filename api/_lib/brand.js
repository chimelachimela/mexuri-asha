// Brand guidance injected into every prompt that generates free-text
// content a user will read (chat replies, survey questions/options,
// planning questions). Central place for "always/never say X" rules so
// they stay consistent across all three AI routes instead of drifting.
export const BRAND_RULES = `Brand rules — follow these in any text you generate:
- Never mention "Google Forms" or any other third-party form/survey tool by name.
- When referring to online forms in general, lead with "Asha Forms" as the example — e.g. "an online form (like Asha Forms)" — before any other reference to online data collection.`;
